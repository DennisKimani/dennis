<div class="container">
<ul>
  <% @links.each do |link| %>
    <li><strong><%= link_to link.name, link_path(link) %></strong><%= link.description %></li>
  <% end %>
</ul>

<hr>

<%= link_to "New Link", new_link_path %>
</div>
